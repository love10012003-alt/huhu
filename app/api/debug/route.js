import { NextResponse } from 'next/server'; import { createClient } from '@supabase/supabase-js'; export const dynamic='force-dynamic'; export async function GET(){ const url=process.env.SUPABASE_URL||process.env.NEXT_PUBLIC_SUPABASE_URL; const skey=process.env.SUPABASE_SECRET_KEY||process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY; const akey=process.env.AVIATIONSTACK_KEY||process.env.AVIATION_KEY; let supabaseCheck={ok:false,error:'',tableExists:false,upsertOk:false,upsertError:''}; try{ const supabase=createClient(url,skey); const {data,error}=await supabase.from('flight_cache').select('iata').limit(1); if(error){ supabaseCheck.error=error.message } else { supabaseCheck.ok=true; supabaseCheck.tableExists=true } }catch(e){ supabaseCheck.error=e.message } let aviationCheck={ok:false,error:'',count:0}; try{ const res=await fetch(`https://api.aviationstack.com/v1/flights?access_key=${akey}&arr_iata=SGN&limit=5`,{cache:'no-store'}); const j=await res.json(); if(j.data) { aviationCheck.ok=true; aviationCheck.count=j.data.length } else { aviationCheck.error=JSON.stringify(j).slice(0,200) } }catch(e){ aviationCheck.error=e.message } return NextResponse.json({env:{SUPABASE_URL:!!process.env.SUPABASE_URL,SUPABASE_SECRET_KEY:!!process.env.SUPABASE_SECRET_KEY,AVIATIONSTACK_KEY:!!process.env.AVIATIONSTACK_KEY},supabase:supabaseCheck,aviation:aviationCheck}); }